Joseph:
	swift code for ball bouncing + "bar" setting + openCV + OSC sending

Jacob:
	swift code for camera background  + openCV + OSC sending

Zhi:
	lua code for the first version of ball dropping (abandoned now) + writeups

Cheng:
	Lua code for slave device + OSC receiving + sound generating
